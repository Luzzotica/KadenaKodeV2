import 'package:flutter/material.dart';

class MultisigPage extends StatefulWidget {
  const MultisigPage({
    super.key,
  });

  @override
  MultisigPageState createState() => MultisigPageState();
}

class MultisigPageState extends State<MultisigPage> {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Multisig Page'));
  }
}
